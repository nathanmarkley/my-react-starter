# my-react-starter

A simple React starter project to edit and commit from iPad with Textastic, Working Copy, and GitHub Codespaces.

## Scripts

- `npm start` — Run the app in development mode
- `npm build` — Create a production build